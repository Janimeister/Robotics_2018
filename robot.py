import serial
import time

def main():
    """
    connected = False
    ser = serial.Serial("COM4", 9600)
    if ser:
        connected = True
        while (connected):
            ser.write(b'1')
    ser.close()
    """

    
main()
